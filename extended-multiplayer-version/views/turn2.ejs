<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Result Page</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <div class="container d-flex align-items-center justify-content-center">
    <div class="col-8">
      <body>
        <h3><%= name1 %> chose <%= choice1 %></h3>
        <h3><%= name2 %> chose <%= choice2 %></h3>
        <% if(winner == "player1"){ %>
        <h3>Congrats! <%= name1 %> win!</h3>
        <% } else if(winner == "none") { %>
        <h3>Alas, a tie!</h3>
        <% } else if(winner == "player2"){ %>
        <h3>Congrats! <%= name2 %> win!</h3>
        <% } %>
        <form action="/game2-1" method="get">
          <input id="restart-button" type="submit" value="Play Another Round" />
        </form>
        <form action="/" method="get">
          <input id="back-button" type="submit" value="Go Back To Main Page" />
        </form>
      </body>
    </div>
  </div>
</html>
